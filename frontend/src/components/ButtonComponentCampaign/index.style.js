import styled from 'styled-components';

export default styled.div`
  .button-functions {
    position: fixed;
    bottom: 0;
    right: 0;
    margin: 20px;
    display: flex;
    justify-content: flex-end;
    z-index: 100000;
  }
  .button {
    margin-right: 10px;
  }
`;
